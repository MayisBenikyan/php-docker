name: Php-docker-action

on: 
  push:
    branches:
      -  master

jobs:
  build-image:
    runs-on: ubuntu-latest
    
    steps:
      - name: Build the Docker Image
        run: docker build . -f Dockerfile -t php-docker
